{% extends "basefront.html.twig" %}

{% block body %}
     <div class="row mt-3">
        <div class="col">
            <h1 class="text-primary mt-5">{{ repa.nom }}</h1>
            <h5> {{ repa.datecreationstring }}</h5>
            <h5> catégorie </h5>
            <h5>Temps de préparation : {{ repa.duree }} min</h5>
            {% if repa.note>=10 %}
                <h5 class="text-success mt-5">note :{{ repa.note }}/20</h5>
            {% else %}
                <h5 class="text-danger mt-5">note :{{ repa.note }}/20</h5>
            {% endif %}                 
        </div>
        <div>
            <!-- emplacement photo -->       
        </div>
    </div>
    <div class="row mt-5">
        <div class="col md-12">
            <p><strong>Instruction :</strong></p>
            <p>
                {{ repa.instruction }}
            </p>
        </div>
    </div>
{% endblock %}