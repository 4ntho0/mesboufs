{% extends "baseadmin.html.twig" %}

{% block body %}

    <form class="form-inline mt-1" method="POST" action="{{ path('admin.categorieAliment.ajout') }}">
        <div class="form-group mr-1 mb-2">
            <h5 class="card-title mb-3">Ajouter une categorie d'aliment</h5>
            <input type="text" class="sm" name="nom">
            <button type="submit" class="btn btn-primary mb-2 btn-sm">Ajouter</button>
        </div>
    </form>   
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    Nom
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            {% for categorieAliment in categoriesAliment %}
                <tr>
                    <td>
                        {{ categorieAliment.nom }}
                    </td>
                    <td>
                        <a href="{{ path('admin.categorieAliment.suppr', {id:categorieAliment.id}) }}" 
                           class="btn btn-danger" 
                           onclick="return confirm('Etes-vous sûr de vouloir supprimer {{ categorieAliment.nom }} ?')">
                            Supprimer
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    
{% endblock %}
